# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# cloudflare (OpenNext/wrangler)
.open-next/
.wrangler/
.dev.vars
cloudflare-env.d.ts

# typescript
*.tsbuildinfo
next-env.d.ts
기획.md
# GSC 서비스 계정 키 (절대 커밋 금지)
gsc-service-account.json
*-service-account.json
helpful-reactor-*.json

# MCP 설정 (Supabase 액세스 토큰 포함 — 커밋 금지)
.mcp.json
scripts/usage-events.sql
stats/
*.sql
/tests/daily-sql.mjs
# 개인정보 — 절대 커밋 금지
member-emails.csv

# 마이그레이션 작업 노트 (로컬 전용)
CLOUDFLARE_MIGRATION.md
.storage-backup/
